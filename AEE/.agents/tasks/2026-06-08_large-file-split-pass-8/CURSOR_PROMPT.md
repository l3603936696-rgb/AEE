# CURSOR_PROMPT.md - large-file-split-pass-8

No Cursor implementation was requested for this pass.

Codex performed a grouped mechanical extraction:

- Move source profile update, semantic residue injection, reply-drive injection,
  and familiarity suppression into `src/daemon/source_tick.py`.
- Keep daemon tick behavior unchanged.
- Keep `_src_id` returned to `tick_engine.py`.
- Update daemon documentation and system index.
