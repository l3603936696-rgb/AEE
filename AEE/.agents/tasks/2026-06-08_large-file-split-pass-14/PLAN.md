# Plan

1. Extract `_ANCHOR_BASELINE`, `_anchor_alignment`, and `match_anchor_expression` to `language_anchor_match.py`.
2. Import `match_anchor_expression` from `language_training.py`.
3. Compile and smoke-test both entrypoints.
4. Update `XIA_SYSTEMS.md`.
