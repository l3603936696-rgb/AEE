# Cursor Prompt

Review the pass-13 narrative split for behavioral regressions.

Check:
- `narrative_fragments.py` imports `_build_context` correctly.
- `narrative_context.py` preserves lookup tables and context construction.
- Wakeup template registration and softmax sampling stayed unchanged.

Do not edit runtime data, logs, caches, models, or secrets.
