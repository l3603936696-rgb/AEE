# Plan

1. Extract narrative lookup tables and `_build_context` to `narrative_context.py`.
2. Import `_build_context` from `narrative_fragments.py`.
3. Compile and smoke-test the narrative expression entrypoint.
4. Update `XIA_SYSTEMS.md`.
