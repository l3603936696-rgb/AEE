# Plan

1. Extract `_nl_extract_*` helpers to `tools_nl_extractors.py`.
2. Import `_nl_extract_tools` from the new module.
3. Compile and smoke-test `extract_tool_calls`.
4. Run target tests.
5. Update `XIA_SYSTEMS.md`.
