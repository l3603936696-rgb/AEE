# PLAN.md - large-file-split-pass-7

## Plan

1. Extract covariance tracker update into `covariance_update.py`.
2. Extract reading intake and sentence extraction into `reading_cycle.py`.
3. Extract StatePatternMemory tick into `state_pattern_tick.py`.
4. Replace inline blocks with helper calls in the same order.
5. Update daemon documentation and system index.
6. Run focused compile/tests and record results.
