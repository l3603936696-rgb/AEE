# Archived Frontend Experiment

This folder keeps old Cursor task notes for the removed Electron/Vite frontend.

The current repository does not ship or maintain a frontend. Runtime interaction
is through the daemon, `channel/`, local HTTP/IPC clients, and `reach_client.py`.

Keep these notes only as historical reference. Do not treat paths under
`frontend/` as active implementation targets.
