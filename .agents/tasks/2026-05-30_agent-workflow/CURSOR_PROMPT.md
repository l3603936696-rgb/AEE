# Cursor Handoff: agent-workflow

No implementation handoff is needed for this setup task. Codex is creating the
initial workflow files directly.

For future tasks, Cursor should read `.agents/workflow/README.md` and the task
directory files before editing code.
